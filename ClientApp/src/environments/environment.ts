export const environment = {
  production: false,
  // Dev: browser stays same-origin (localhost:4200) and proxy.conf.json
  // forwards /api to the Gateway — see angular.json's serve.options.proxyConfig.
  apiBaseUrl: '',
};
